function intersectionofarrays(array1,array2){

    let obj1 ={};
    let obj2 = {};

    for(i of array1){
        (obj1[i]) ? obj1[i]++ : obj1[i] = 1
    }

    for(j of array2){
        (obj2[j]) ? obj2[j]++ : obj2[j] =1 
    }
    
    for(k )
}