let array = [1,2,3,4,5,6]

// let res = array.splice(0,3) // first index is 

// console.log(`removed elements ${res} and other elments on original array ${array}`);

let res1 = array.splice(-1,2)

console.log(`removed elements ${res1} and other elments on original array ${array}`); 